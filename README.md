# RunningHub AI Client (RunningHub AI 客户端)

### [可以在这里下载](https://pan.quark.cn/s/81d55c2f47f4) 或者右边 Releases 中下载最新版本

- **注册福利**: [国内点击注册送1000RH币](https://www.runninghub.cn/?inviteCode=rh-v1123) | [海外点击注册送1000RH币](https://www.runninghub.ai/?inviteCode=rh-v1123)

RunningHub AI Client 是一个基于 React 和 Vite 构建的现代化桌面端应用，专为 RunningHub AI 服务设计。它提供了一个直观的图形化界面，使用户能够轻松配置工作流、批量运行任务、实时监控进度以及自动管理生成的内容。

<img width="2571" height="1764" alt="rh11" src="https://github.com/user-attachments/assets/4d7b5fc0-e251-4c8e-b788-f0b0446ef283" />

## ✨ 核心功能

- **🚀 快速连接**: 通过 API Key 和 WebApp ID 快速连接到 RunningHub 服务，自动获取应用节点配置
- **🎨 可视化参数编辑**: 支持图片上传、文本输入、数值调整、选项列表及开关等多种参数类型
- **⚡ 批量任务处理**: 支持导入批量数据，实现多任务并发运行，大幅提升工作效率
- **📊 实时状态监控**: 实时查看任务进度、成功/失败状态及详细的错误日志
- **💾 自动保存系统**: 可配置本地存储目录，自动下载并分类保存任务生成的图片、视频等文件
- **📝 历史记录管理**: 自动记录所有任务的运行历史和输出结果，支持随时回溯查看
- **🔄 自动更新**: 支持自动检测新版本并一键更新
- **🌙 深色模式**: 默认启用深色主题界面，提供专业且舒适的视觉体验

---

## 🚀 快速开始

### 用户安装

直接从 [Releases 页面](https://github.com/yzz05220-rgb/RunningHub-AI-Client/releases) 下载最新的 `.exe` 安装包，双击运行即可。

### 开发者本地运行

**环境要求**
- [Node.js](https://nodejs.org/) v18+
- npm 或 yarn

**安装步骤**

```bash
# 1. 安装依赖
npm install

# 2. 启动开发环境
npm run dev

# 3. 构建生产版本
npm run electron:build
```

---

## 📖 使用说明

1. **配置连接**: 启动应用后，在左侧面板输入 `API Key` 和 `WebApp ID`，点击"下一步"加载应用配置
2. **调整参数**: 在中间编辑器面板修改各节点参数（上传图片、修改提示词等）
3. **执行任务**:
   - **单次运行**: 点击底部"运行"按钮
   - **批量运行**: 点击"批量运行"设置多组参数并发处理
4. **获取结果**: 任务完成后结果展示在右侧面板，开启自动保存可自动下载到指定目录

---

## 🔄 自动化版本发布流程

### 概述

本项目已实现**完全自动化**的版本发布流程。您只需执行几条 Git 命令，剩下的所有工作（构建、打包、发布、更新元数据生成）都由 GitHub Actions 自动完成。

### 发布新版本步骤

#### 1. 修改版本号

编辑 `package.json`，将 `version` 字段增加：

```json
{
  "version": "1.3.7"  // 从 1.3.6 改为 1.3.7
}
```

#### 2. 更新变更日志（推荐）

在 `CHANGELOG.md` 中记录本次更新：

```markdown
## [1.3.7] - 2026-01-18
### 新增
- 添加了 XXX 功能
### 修复
- 修复了 XXX 问题
```

#### 3. 提交并打标签

```bash
git add .
git commit -m "chore: release v1.3.7"
git tag v1.3.7
git push myorigin main --tags
```

#### 4. 等待自动化完成

推送 Tag 后，GitHub Actions 会自动执行：

| 阶段 | 操作内容 | 预计时间 |
|------|----------|----------|
| **构建** | 安装依赖 → Vite 构建 → electron-builder 打包 | 5-10 分钟 |
| **发布** | 创建 GitHub Release → 上传安装包和 `latest.yml` | 1-2 分钟 |
| **客户端检测** | 已安装客户端自动检测更新并提示用户 | 实时 |

### 验证发布

1. **GitHub Actions**: 访问仓库 → Actions → 查看最新工作流是否绿色 ✅
2. **Releases 页面**: 确认 Release 已发布（非 Draft），附件包含：
   - `RunningHub-AI-Setup-x.x.x.exe`
   - `latest.yml`

### 自动更新机制

客户端内置 `electron-updater`，会自动从 GitHub Releases 检测更新：

- **检测时机**: 应用启动时 + 定期检查
- **更新提示**: 发现新版本后右下角弹出提醒
- **一键更新**: 用户点击"立即更新"后自动下载安装

> 💡 **关键配置** (`package.json` 的 `build.publish`):
> ```json
> "publish": [{
>   "provider": "github",
>   "owner": "yzz05220-rgb",
>   "repo": "RunningHub-AI-Client"
> }]
> ```

### 常见问题

| 问题 | 解决方案 |
|------|----------|
| 构建失败 | 查看 Actions 错误日志，修复后重新打 Tag |
| 客户端检测不到更新 | 确认 Release 非 Draft、`latest.yml` 存在、新版本号更高 |
| 跳过版本号 | 可以，但建议遵循语义化版本规范 |

---

## 🔗 相关链接

- **RunningHub 官网**: [https://www.runninghub.cn](https://www.runninghub.cn)
- **注册福利**: [点击注册送1000RH币](https://www.runninghub.cn/?inviteCode=rh-v1123)
- **项目作者**: [哔哩哔哩 HooTooH](https://space.bilibili.com/527601196)
- **交流群**: QQ 543917943
- **致谢**: [Penguin](https://github.com/PenguinTeo) - API 测试支持

---

⭐ 如果对你有帮助，记得点个 Star 支持一下！

© 2026 RunningHub AI Client. All Rights Reserved.
